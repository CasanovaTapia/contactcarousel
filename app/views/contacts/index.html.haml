%h1 Listing contacts

%table
  %tr
    %th Name
    %th Entity
    %th Phone
    %th Alt phone
    %th Dead phone
    %th Email
    %th Alt email
    %th Dead email
    %th
    %th
    %th

  - @contacts.each do |contact|
    %tr
      %td= contact.name
      %td= contact.entity
      %td= contact.phone
      %td= contact.alt_phone
      %td= contact.dead_phone
      %td= contact.email
      %td= contact.alt_email
      %td= contact.dead_email
      %td= link_to 'Show', contact
      %td= link_to 'Edit', edit_contact_path(contact)
      %td= link_to 'Destroy', contact, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Contact', new_contact_path
